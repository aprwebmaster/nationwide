<%= form_with(model: meeting, local: true) do |form| %>
  <% if meeting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meeting.errors.count, "error") %> prohibited this meeting from being saved:</h2>

      <ul>
      <% meeting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :meeting_name %>
  </div>

   <div class="field">
    <%= form.label :subcontractor %>
    <%= form.text_field :subcontractor, id: :meeting_subcontractor %>
  </div>

  <div class="field">
    <%= form.label :division %>
    <%= form.text_field :division, id: :meeting_division %>
  </div>

  <div class="field">
    <%= form.label :start_time %>
    <%= form.datetime_select :start_time, id: :meeting_start_time %>
  </div>

  <div class="field">
    <%= form.label :end_time %>
    <%= form.datetime_select :end_time, id: :meeting_end_time %>
  </div>
<% if policy(Account.new).create? %>
  <div class="actions">

    <%= form.submit "Create Event" %>
  </div>
  <% end %>
  <br>
<% end %>
